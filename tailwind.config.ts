import type { Config } from 'tailwindcss'

export default {
  theme: {
    extend: {
      fontFamily: {
        sans: ['FUNKEpublish', 'system-ui', 'sans-serif'],
      },
    },
  },
} satisfies Config
